const GREPPER = require("../src/index.js")

async function __main__() {
    // Data = await GREPPER.userInfo(98467, true)
    // console.log(Data)
    // Belt = await GREPPER.userBeltStats(98467, true)
    // console.log(Belt)
    // Search = await GREPPER.searchUsers("wefwef3g54h65yter", true)
    // console.log(Search)
    // Answers = await GREPPER.getAnswers("js loop", true);
    // console.log(Answers)
    // uStats = await GREPPER.userStats(98467, true)
    // console.log(uStats)
    // Comment = await GREPPER.getComments(23);
    // console.log(Comment)
}
__main__()